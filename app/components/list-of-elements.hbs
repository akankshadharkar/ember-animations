<AnimatedContainer>
  {{#animated-each @elements use=transition duration=1000 as |element|}}
    <SingleElement @id={{element}} @obj={{element}} @onClick={{(fn @onFaceClicked element)}} />
  {{/animated-each}}
</AnimatedContainer>