<AnimatedContainer>
  {{#animated-each @elements use=transition duration=500 as |element|}}
    <SingleElement @id={{element.id}} @obj={{element}} @onClick={{(fn @onFaceClicked element)}} />
  {{/animated-each}}
</AnimatedContainer>