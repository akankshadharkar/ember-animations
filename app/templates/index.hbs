{{page-title "Index"}}

<AnimatedTools @hideUntilKeys="Ctrl-Shift-KeyA" />

<Container
  @elements={{@model}}
  @toggleElementsBetweenLists={{this.toggleElementsBetweenLists}}
/>

<ButtonRouteTransition @redirectedRoute="settings" @buttonText="Settings"/>

{{outlet}}